version: '3'

services:
  client:
    build:
      dockerfile: Dockerfile
      context: ./
    labels:
      - "traefik.enable=true"
      - "traefik.frontend.rule=Host:domain.name"
      - "traefik.port=80"
    networks:
      - web
networks:
  web:
    external:
      name: web
